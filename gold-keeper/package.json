{
  "name": "gold-keeper",
  "version": "1.0.0",
  "description": "梦幻西游金币管家 - Cloudflare Pages + D1",
  "scripts": {
    "dev": "wrangler pages dev --local public",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
